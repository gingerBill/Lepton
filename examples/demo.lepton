const a, b, c = 123, 345, 456
// var x = 123
// var y = x * 2
type Vector [3]int
type Bar [3]Foo
type Foo [3]Bar

proc f(): int {
	type SmallInt range -10..10
	var x: int = 123
	y := +23 * 2 / -3 + x;
	// x = add(y, x);

	return 1
}

proc add(x: int, y: int): int {
	return x + y;
}
